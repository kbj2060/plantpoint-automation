# Docker Deployment Configuration
# Use this when running automation inside Docker container

# GPIO Configuration (use real GPIO in production)
USE_REAL_GPIO=true

# Authentication
USERNAME=llewyn
PASSWORD=1234

# Database Configuration (Docker service names)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=plantpoint
DB_USER=postgres
DB_PASSWORD=postgres

# API Configuration (Docker service names)
API_BASE_URL=http://frontend/api
BACKEND_ADDRESS=http://frontend/api
SIGNIN_URL=http://frontend/api/authentication/signin
AUTOMATION_READ_URL=http://frontend/api/automations
INTERVAL_DEVICE_STATES_READ_URL=http://frontend/api/switches/interval-states
ENVIRONMENT_EACH_LATEST_READ_URL=http://frontend/api/environment/each-latest
ENVIRONMENT_TYPE_READ_URL=http://frontend/api/environment/types
SWITCH_EACH_LATEST_READ_URL=http://frontend/api/switches/each-latest
MACHINE_READ_URL=http://frontend/api/device?type=machine
SENSOR_READ_URL=http://frontend/api/device?type=sensor
CURRENT_READ_URL=http://frontend/api/current/read-all

# WebSocket Configuration (Docker service names)
SOCKET_ADDRESS=ws://frontend/ws
SWITCH_SOCKET_ADDRESS=ws://frontend/ws/switch
CURRENT_SOCKET_ADDRESS=ws://frontend/ws/current
WS_HOST=frontend
WS_PORT=80
WS_SWITCH_EVENT=sendSwitchToServer
WS_CURRENT_EVENT=sendCurrentToServer

# MQTT Configuration (Docker service names)
MQTT_HOST=mqtt
MQTT_PORT=1883
MQTT_ID=automation
MQTT_KEEPALIVE=60

# Redis Configuration (Docker service names)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Logging Configuration
LOG_DIR=.logs
LOG_MAX_BYTES=10485760
LOG_BACKUP_COUNT=5

# Thread Configuration
THREAD_CHECK_INTERVAL=60

# Automation Configuration
CURRENT_BUFFER_SIZE=5
TARGET_REQUIRED_COUNT=3
